MONGO_URI=mongodb://localhost:27017/mangalorebayaan
PORT=3000

# If using MongoDB Atlas, set MONGO_URI to your connection string.
# Admin password (choose a strong password). Used by /api/auth/login
ADMIN_PASSWORD=changeme
# JWT secret for signing admin tokens
JWT_SECRET=change_this_secret
